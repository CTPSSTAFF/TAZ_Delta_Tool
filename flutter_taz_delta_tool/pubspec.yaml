name: taz_delta_tool
description: Demonstration of shapefile->geojson usage in Flutter

publish_to: "none"

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  maplibre_gl: ^0.20.0
  turf: ^0.0.10
  r_tree: ^3.0.1
  # archive: ^4.0.7
  archive: ^3.4.9
  path_provider: ^2.1.5
  sqlite3: ^2.7.6
  sqflite: ^2.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - ./assets/geojsons.zip
